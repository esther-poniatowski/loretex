Place the PDF image assets for this integration test under:

tests/fixtures/complex/assets/figs/diagram.pdf

The Markdown uses:

<img src="figs/diagram.svg" width="300">

The converter will map this to `assets/figs/diagram.pdf` during LaTeX compilation.
